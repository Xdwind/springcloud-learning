server:
  port: 7001

eureka:
  instance:
    hostname: localhost #eurek服务端的实例名称
  client:
    register-with-eureka: false #false表示不向服务器注册中心注册自己
    fetch-registry: false #表示自己是注册中心
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/